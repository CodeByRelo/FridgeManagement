@model IEnumerable<FridgeManagement.Models.ServiceCheckup>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CheckupDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Notes)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Fridge)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CheckupDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Notes)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fridge.Model)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ServiceCheckupID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ServiceCheckupID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ServiceCheckupID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
