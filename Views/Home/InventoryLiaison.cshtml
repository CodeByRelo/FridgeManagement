@using Microsoft.AspNetCore.Identity
@using FridgeManagement.Areas.Identity.Data

@inject SignInManager<FridgeManagementUser> SignInManager
@inject UserManager<FridgeManagementUser> UserManager

@{
    var user = UserManager.GetUserAsync(User).Result; // Get the user synchronously (not recommended for long-term use)
    var firstName = user?.FirstName;
}


@{
    ViewData["Title"] = "InventoryLiaison";
    Layout = "~/Views/Shared/_LoggedInLayout.cshtml";
}

<!-- Admin Dashboard Layout -->
<section class="admin-hero">
    <div class="admin-welcome">
        <h2>Hi, @firstName</h2>
        <p>Receive new fridges, "scrap" fridges and create purchase requests</p>
    </div>
</section>

<section class="admin-links">
    <div class="container-fluid py-5">
        <div class="container">
            <div class="row g-4">
                <div class="col-lg-6 wow fadeIn" data-wow-delay="0.1s">
                    <div class="feature-item position-relative bg-primary text-center p-3">
                        <div class="border py-5 px-3">
                            <a href="/FridgeActions/Index" class="admin-btn">Inventory</a>
                        </div>
                    </div>
                </div>
                <div class="col-lg-6 wow fadeIn" data-wow-delay="0.3s">
                    <div class="feature-item position-relative bg-primary text-center p-3">
                        <div class="border py-5 px-3">
                            <a href="/PurchaseRequests/Index" class="admin-btn">Purchase Requests</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

